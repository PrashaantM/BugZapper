# BugZapper

This project was initially committed as a completed snapshot, so the repository contains a single initial commit. Ongoing development (if any) will be tracked with incremental commits following standard Git workflow practices.

3D browser game built from scratch in WebGL + custom GLSL shaders, no external 3D libraries. 60 FPS with adaptive difficulty.
